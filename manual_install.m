% manually installs xolotl
% does not use MATLAB's toolbox because
% versions installed that way are slower (really)
% UX elements are slower, which means
% manipulate is too slow to use

addpath(pwd)
addpath('../cpplab')
addpath('../puppeteer')
addpath(genpath('../srinivas.gs_mtools/src'))